# Lighthouse audits — PWA closeout (#9)

Recorded 2026-08-22 against `dist/read-punjabi/browser` served locally
(gzip, mirroring Cloudflare Pages), Chrome 151 headless, emulated mobile,
Lighthouse 12.

## Scores

| Route     | Performance | Accessibility | Best Practices | SEO |
| --------- | ----------- | ------------- | -------------- | --- |
| `/`       | **97**      | **99**        | **100**        | **100** |
| `/level/2`| 77          | 96            | 100            | 100 |

The home page meets the ≥90 target across all four categories.
`/level/2` trails on Performance because its largest contentful paint lands
only after Angular boots and fetches the level's lesson JSON client-side;
a fresh-profile first load is not service-worker-controlled, so this is
pre-existing behaviour rather than a PWA regression. Worth a follow-up if
route-level budgets are ever formalised.

## Offline smoke test

Automated via headless Chrome (puppeteer-core): service worker active,
59 course-content URLs prefetched into Cache Storage, then network disabled
via CDP — Levels (`/level/2`), Lessons (`/lesson/letter-1`), Random Practice
(`/level/2/random`), and Quiz (`/quiz/1/1/active`) all rendered usable
offline. 6/6 checks passed.

## Manual verification still owed

- Install flow on a physical mobile browser (Add to Home Screen, launch standalone).
